apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.apps.backend.name }}
spec:
  selector:
    app: {{ .Values.apps.backend.name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
